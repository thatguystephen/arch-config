# Host configuration for don-deck
# Config for my steamdeck

host: don-deck
description: Packages specific to don-deck

# Import shared configurations (optional)
# Example:
# import:
#   - hosts/shared/laptop-common.yaml

# Enabled modules
enabled_modules: []

# Host-specific packages
packages: []

# Exclude packages from base or modules
exclude: []

# Settings
flatpak_scope: user
auto_prune: false
backup_tool: timeshift
snapper_config: root
