# Hyprland Configuration
# https://wiki.hypr.land/Configuring/

# ==================
# MONITOR CONFIG
# ==================
# monitor = eDP-2, 2560x1600@239.998993, 2560x0, 1, vrr, 1
#monitor = , preferred,auto,auto
source = ~/.config/hypr/monitors.conf

# ==================
# ENVIRONMENT VARS
# ==================
env = QT_QPA_PLATFORM,wayland
env = ELECTRON_OZONE_PLATFORM_HINT,auto
env = QT_QPA_PLATFORMTHEME,gtk3
env = QT_QPA_PLATFORMTHEME_QT6,gtk3
env = TERMINAL,kitty
env = XCURSOR_THEME,Bibata-Modern-Ice
env = XCURSOR_SIZE,24

cursor {
    default_monitor =
}

# ==================
# STARTUP APPS
# ==================
exec-once = bash -c "wl-paste --watch cliphist store &"
exec-once = /usr/lib/mate-polkit/polkit-mate-authentication-agent-1
exec-once = pypr
exec-once = hyprpm reload
exec-once = hyprctl setcursor Bibata-Modern-Ice 24

# Shells Exec Command
exec-once = dms run

# Communication apps on DP-3
exec-once = [workspace 1 silent; monitor DP-3] discord
exec-once = [workspace 1 silent; monitor DP-3] telegram-desktop

# ==================
# INPUT CONFIG
# ==================
input {
    kb_layout = us
    numlock_by_default = true
}

# ==================
# GENERAL LAYOUT
# ==================
general {
    gaps_in = 5
    gaps_out = 10
    border_size = 2

    col.active_border = rgba(f5c2e7ff)
    col.inactive_border = rgba(d0d0d020)

    layout = scrolling
}

# ==================
# DECORATION
# ==================
decoration {
    rounding = 2

    active_opacity = 0.98
    inactive_opacity = 0.8

    shadow {
        enabled = true
        range = 30
        render_power = 5
        offset = 0 5
        color = rgba(00000070)
    }
}

# ==================
# ANIMATIONS
# ==================
animations {
    enabled = true

    animation = windowsIn, 1, 3, default
    animation = windowsOut, 1, 3, default
    animation = workspaces, 1, 5, default, slidevert
    animation = windowsMove, 1, 4, default
    animation = fade, 1, 3, default
    animation = border, 1, 3, default
}

# ==================
# LAYOUTS
# ==================
dwindle {
    preserve_split = true
}

master {
    mfact = 0.5
}

scrolling {
}

# ==================
# MISC
# ==================
misc {
    disable_hyprland_logo = true
    disable_splash_rendering = true
    vrr = 1
}

# ==================
# PLUGIN CONFIG
# ==================
# Hyprscrolling plugin configuration
# Plugin is managed by hyprpm (enable/disable via toggle script)
plugin {
    hyprscrolling {
        column_default_width = onehalf
        focus_wrap = false
    }

    hyprexpo {
        columns = 3
        gap_size = 5
        bg_col = rgb(111111)
        workspace_method = center current # [center/first] [workspace] e.g. first 1 or center m+1

        enable_gesture = false
    }
}

# ==================
# WORKSPACE RULES
# ==================
workspace = 1, layout:scrolling

# ==================
# WINDOW RULES
# ==================
windowrulev2 = tile, class:^(org\.wezfurlong\.wezterm)$

windowrulev2 = rounding 12, class:^(org\.gnome\.)
windowrulev2 = noborder, class:^(org\.gnome\.)

windowrulev2 = tile, class:^(gnome-control-center)$

windowrulev2 = tile, class:^(nm-connection-editor)$

windowrulev2 = float, class:^(gnome-calculator)$
windowrulev2 = float, class:^(pavucontrol)$
windowrulev2 = float, class:^(galculator)$
windowrulev2 = float, class:^(blueman-manager)$
windowrulev2 = float, class:^(org\.gnome\.Nautilus)$
#windowrulev2 = float, class:^(steam)$
windowrulev2 = float, class:^(xdg-desktop-portal)$

windowrulev2 = float, class:^(firefox)$, title:^(Picture-in-Picture)$
#windowrulev2 = float, class:^(zoom)$

windowrulev2 = opacity 0.9 0.9, floating:0, focus:0

# Pyprland scratchpad
$dropterm = class:^(kitty-dropterm)$
windowrulev2 = float, $dropterm
windowrulev2 = workspace special silent, $dropterm

layerrule = noanim, ^(quickshell)$

# ==================
# KEYBINDINGS
# ==================
$mod = SUPER

# === Shell Switcher ===
bind = CTRL SHIFT, S, exec, ~/.config/hypr/Scripts/switch-shell.sh

# === Layout Toggle ===
bind = $mod CTRL, S, exec, ~/.config/hypr/Scripts/toggle-layout.sh

# === Universal Keybinds (work for all shells) ===
source = ~/.config/hypr/keybinds-universal.conf

# === Shell-Specific Keybinds (dynamically loaded via symlink) ===
source = ~/.config/hypr/keybinds-active.conf
