# Homelab module
# Container orchestration and homelab services

description: Homelab server tools and container management

packages:
  # Container management
  - docker
  - docker-compose
  - portainer-bin

conflicts: []
post_install_hook: "scripts/setup-immich.sh"
hooks_behavior: skip # or "always", "skip", "once" (default)
